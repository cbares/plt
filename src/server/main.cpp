#include <iostream>
#include <state.h>
#include <server.h>
#include <cstring>
#include <random>

using namespace std;
using namespace state;
using namespace server;

int main(int argc,char* argv[])
{   
    srand(time(NULL));

    return 0;
}
